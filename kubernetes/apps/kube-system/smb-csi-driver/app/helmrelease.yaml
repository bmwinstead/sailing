---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: smb-csi-driver
spec:
  chart:
    spec:
      chart: csi-driver-smb
      version: 1.20.1
      sourceRef:
        kind: HelmRepository
        name: csi-driver-smb
  interval: 1h
  values:
    controller:
      replicas: 1
