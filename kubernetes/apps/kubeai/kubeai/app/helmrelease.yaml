---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kubeai
spec:
  chart:
    spec:
      chart: kubeai
      version: 0.23.2
      sourceRef:
        kind: HelmRepository
        name: kubeai
  interval: 1h
  values: {}
