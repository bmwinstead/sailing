---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gpu-operator
spec:
  chart:
    spec:
      chart: gpu-operator
      version: v26.3.2
      sourceRef:
        kind: HelmRepository
        name: nvidia
  interval: 1h
  values: {}
